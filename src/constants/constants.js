const CONNECTION_STATUS = ['interested', 'ignored', 'accepted', 'rejected'];

const ALLOWED_CONNECTION_STATUS = ['interested', 'ignored'];

module.exports = {
    CONNECTION_STATUS,
    ALLOWED_CONNECTION_STATUS
}